app_identifier("com.priyansh.mala")

# Authentication is handled by an App Store Connect API key inside the Fastfile
# (app_store_connect_api_key), so no apple_id / itc_team_id / team_id is stored
# here. This keeps the repo free of personal Apple ID credentials.
